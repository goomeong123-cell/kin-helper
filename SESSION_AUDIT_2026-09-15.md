## 추가 안전 수정 — 로컬 적용, 미배포

- 자동 로그인은 제출 버튼을 먼저 선택한 뒤 한 번만 클릭한다. 클릭 오류 후 다른 선택자/Enter로 재제출하지 않는다.
- 워밍업은 실제 페이지에서 초기 인증 상태를 확인한다. 불명확 상태, 남은 인증 쿠키와 로그아웃 표시의 충돌, 세션 중 인증 상태 변경은 AUTH_STOP으로 중단한다.
- 페이지 이동·질문 읽기·스크롤 등 작업 사이에 인증/보호조치 검사를 수행한다. 검사는 연속 실시간 감시가 아니며 진행 중인 대기 이후 다음 검사에서 멈춘다.
- 워밍업 오류 시 예약을 해제해 스케줄러의 반복 시도를 막는다. 워밍업 기간 종료는 계정 안전성 판정이 아니며 답변 시작의 기존 인증 검사는 유지된다.
- 검증: npm run build, tests/session-safety.mjs, tests/offline-configuration-audit.mjs 통과. 실제 계정/네이버 요청 없음. 보호조치 원인은 미확정.
- 기존 미배포 워밍업 변경과 같은 파일에 포함되어 있으므로 릴리즈 시 변경 범위를 검토해야 한다.

## 수정 결과 — 로컬 적용, 미배포

- Chrome/Electron의 브라우저 정보·UA·Client Hints 덮어쓰기 제거. Chrome 샌드박스 활성화, 계정별 프로필과 프록시는 유지.
- 공통 proxyFor 검증 추가: 계정/호스트/포트가 없거나 잘못되면 네이버 요청 전에 중단. 직접 접속 fallback 제거.
- 상세 조회 및 진단 요청 HTTPS 인증서 검사를 활성화. 시계 진단도 해당 계정 프록시 사용.
- 계정 없는 답변 초안 생성은 저장된 질문 내용만 사용. 계정 선택 후 질문 수집·상세 읽기 가능.
- 빈/누락 비밀번호는 유지. 명시적 삭제 체크와 저장 시에만 삭제. 암호화 실패/입력 충돌 시 기존 값 유지.
- 브라우저 열기 성공 문구, 진단 결과의 과도한 안전 보장 문구 수정.
- 검증: 타입/번들 빌드, offline-configuration-audit, session-safety, 실제 React 폼의 빈값/삭제/저장실패 테스트 및 화면 검토. 운영 데이터·계정 접속 없음.
- 아래는 수정 전 발견 사항이다. 보호조치의 직접 원인은 여전히 미확정.

# 로그인/접속 경로 로컬 감사 — 2026-09-15

대상: v0.9.5 기준 코드 및 기존 미커밋 변경이 있는 로컬 작업 폴더.
범위: 읽기, 모의 외부 경계 테스트, 계정과 분리된 Chrome 테스트. 실제 네이버 계정/VM/프록시 접속 없음. 운영 DB를 열거나 수정하지 않음. 앱 코드 수정·배포 없음.

## 결론

클라이언트의 결함/보안 설정 문제는 확인했지만 네이버 보호조치의 직접 원인, 발동 시각은 알 수 없다. 지문 해시 차이·출구 IP 반복 측정·워밍업은 계정 안전을 증명하지 않는다.

## 확인 결과

1. **실제 Chrome 실행과 기본 브라우저 설정은 서로 다른 의미다.**
   - electron/pwlogin.ts buildContextOptions / applyStealthInit: 시스템 Chrome을 실행하면서 브라우저 정보 일부 및 실행 옵션을 변경한다.
   - 네이버가 변경된 정보를 판단에 사용했는지는 서버 자료가 없어 확인할 수 없다.
   - 더 정교한 위장이 아닌, 불필요한 브라우저 정보 변경 제거를 우선 검토한다. 기본 설정 복귀 역시 보호조치 해소 보장은 아니다.
2. **프록시 필수 규칙이 모든 네이버 요청에 적용되지 않는다.**
   - pwlogin.ts getAccountContext: 프록시 없으면 Chrome 실행 차단. 테스트 확인.
   - naver.ts getProxyOnlySession: 계정/프록시 없으면 direct 경로. questions:collect의 accountId는 선택값이다.
   - naver.ts fetchQuestionDetail: 계정 없으면 일반 fetch. 실제 함수를 모의 fetch로 호출해 확인.
   - ipc.ts answers:generate / answers:generateAll -> doGenerate -> fetchQuestionDetail 경로도 detailProxy 없이 호출될 수 있다.
   - 이 요청은 로그인 쿠키를 넘기지 않으므로, 이것만으로 계정 세션 탈취/보호조치를 일으켰다고 결론 내릴 수 없다.
3. **HTTPS 인증서 오류를 무시하는 별도 요청이 있다.**
   - naver.ts fetchQuestionDetail와 pwlogin.ts 프록시/시계 진단 요청: ignoreHTTPSErrors=true.
   - Chrome 로그인 buildContextOptions에는 이 설정이 없다. 따라서 로그인 비밀번호 유출을 확인한 것은 아니다.
   - 인증서 검사를 복구하고 실패를 명확히 표시하는 것이 적절하다.
4. **계정 편집이 기존 비밀번호를 지울 수 있다.**
   - Accounts.tsx는 빈 비밀번호가 기존 값 유지라고 안내하고, 초기값 빈 문자열을 포함한 f를 통째로 update에 전달한다.
   - ipc.ts accounts:update는 빈 문자열이면 naver_pw=NULL로 저장한다.
   - 실제 IPC 핸들러를 모의 DB로 실행해 확인했다. Chrome에 이미 존재하는 로그인 쿠키를 지우는 코드와는 별개다.
5. **Chrome 종료 시 세션 쿠키가 사라질 수 있다.**
   - tests/auth-startup-repro.mjs: 임시 프로필에 영구 쿠키와 세션 쿠키를 넣고 재실행.
   - 영구 NID_AUT는 남고, 세션 NID_SES는 사라지는 결과를 확인했다. 실제 계정에서 발급된 쿠키의 속성은 읽지 않았다.
   - v0.9.5는 로그인 후 Chrome을 닫지 않고 작업으로 넘긴다. 보호조치로 서버가 무효화한 세션을 복원하는 기능은 아니다.
6. **현재 로그인 경로의 쿠키 복사는 제거돼 있다.**
   - ipc.ts에서 importCookiesToAccountSession / persistAccountLogin 호출 없음.
   - naver.ts에는 옛 함수가 남아 있으나 현재 로그인/작업 IPC 경로에서 호출되지 않는다.
7. **진단 문구가 실제 검증 범위보다 강하다.**
   - 반복 IP 요청의 응답이 한 종류이면 stable=true. 일부 요청이 실패해도 성공 응답 하나만으로 true일 수 있다.
   - 헤더 검사 통과는 특정 에코 서비스의 응답에 대한 관찰이다. 로그인 서버의 계정 위험 판단이나 과거 접속 상태를 증명하지 않는다.

## 재현 도구

- node tests/offline-configuration-audit.mjs: 실제 export/IPC 함수에 모의 네트워크와 모의 DB를 주입. Chrome 프록시 옵션 전달/미설정 차단, 상세 조회 direct 경로, 별도 요청 TLS 설정, 빈 비밀번호 삭제 검증.
- node tests/session-safety.mjs: 컨텍스트 중복 실행 방지, 인증 확인, 로그인 창 재사용. 테스트 페이지 요청은 로컬로 대체.
- node tests/auth-startup-repro.mjs: 로그인 UI 지연 및 임시 프로필 재실행 확인. 실제 계정 사용 없음.

## 권장 다음 작업 (이 감사에서 적용하지 않음)

1. 브라우저 정보 덮어쓰기를 제거하고 지원되는 기본 브라우저 설정으로 단순화한다.
2. 네이버 요청의 프록시 미설정 시 동작을 명시적으로 통일하고, 인증서 오류 무시를 제거한다.
3. 빈 비밀번호 보존과 명시적 삭제 동작을 구분한다.
4. 진단 결과를 관측 사실로만 표시한다. 보호조치 예방/안전/비탐지 보장 표현을 제거한다.
5. 로컬 검증 후 사용자에게 결과와 남은 한계를 보고한다. 새 계정을 소모하는 반복 테스트는 하지 않는다.
