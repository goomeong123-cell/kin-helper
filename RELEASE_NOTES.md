# v0.9.7 — 워밍업 기본 꺼짐 + 로그인·세션 판정을 카페포스터와 동일하게

- **워밍업 기능 기본 꺼짐.** 설정 탭에 스위치(실험 기능). 꺼져 있으면 스케줄러 정지, 버튼·진행 표시 숨김, 워밍업 예약된 계정도 답변 투입에서 제외되지 않음.
  근거: 운영 중인 카페포스터는 같은 프록시(하이온넷)·같은 출처 계정 18개를 워밍업 없이 정상 운영 중(9/14~16 진단 131건 중 로그인·보호조치 0건). 워밍업만 돌린 계정(답변 0·수동 조작 0)이 보호조치를 받은 실사례 있음. 예방 효과는 확인된 바 없음.
- **로그인 판정 = 카페포스터 규칙.** `NID_AUT` 쿠키가 있으면 로그인. 없으면 홈을 한 번 띄워 갱신을 기다린 뒤 재판정. 그래도 없을 때만 로그아웃(AUTH_STOP, 재로그인 안 함).
  v0.9.4~0.9.6 은 `NID_AUT`+`NID_SES`+화면 계정표시가 전부 있어야 통과라, `NID_SES` 하나 빠진 정상 세션도 "확인 불가"로 멈추고 사람에게 재로그인을 유도했다.
- **로그인 절차 = 카페포스터 절차.** 로그인 페이지(`nidlogin.login`)로 직접 이동 → 클릭 후 `fill` 입력(한 글자씩 타이핑하면 네이버 자동완성이 끼어들어 글자가 뒤섞이는 사고 방지) → 보이는 버튼 하나만 한 번 클릭 → 최대 25초 쿠키 폴링. 제출이 불명확하면 다른 버튼/Enter 재시도 없음.
- 검증: `npm run build`, `tests/session-safety.mjs`(쿠키 우선 규칙으로 갱신), `offline-configuration-audit`, `accounts-form` 통과. 실계정·네이버 접속 없음. **카페포스터 코드·DB는 읽기만 했고 수정하지 않음.**
- 판정 실험(사용자 진행): 구매 계정 1개 로그인만 → 활동 없이 3일 → 앱 재시작 후 로그인 유지 확인. 살아있으면 워밍업이 원인, 죽으면 로그인 경로 자체를 카페포스터 모듈로 교체.

# v0.9.6 — 위장 최소화(A안) + 워밍업 리듬 사람화 + Codex 안전 수정 통합

- **[프록시 진단]에 회선 종류 표시.** 출구 IP의 공개 등록 대역명(RDAP)으로 `통신사 회선 / 서버 호스팅(IDC) 대역 / 판별 불가`를 보여줍니다. 호스팅 대역이면 빨간 경고. 조회는 공개 레지스트리에만 하며 네이버·계정과 무관합니다. (`tests/ip-line.mjs`)
- **세션 쿠키 영구화 복구(v0.9.4에서 삭제됐던 것).** 만료 없는 네이버 세션 쿠키(`NID_SES` 등)에 2분마다·Chrome 닫기 직전 30일 만료일을 붙여 디스크에 남깁니다. 값은 바꾸지 않으며 서버는 만료일을 볼 수 없습니다. 근거: v0.9.4 업데이트 직후 Chrome 강제 종료 → 로그아웃 → 재로그인 → 보호조치가 실제로 발생. `tests/session-persist.mjs`로 재실행 후 쿠키 생존을 확인(대조군 포함).
- **브라우저 정보 변경은 `navigator.webdriver` 마스킹 하나만 남깁니다.** Playwright가 띄운 Chrome은 실행 인자와 무관하게 `webdriver=true`로 보이는 것을 about:blank에서 실측(네이버 접속 없음)했고, 프로토타입 getter로 `false`가 되며 own-property 흔적이 없는 것도 확인했습니다. 코어/메모리/화면/UA/Client Hints 위장은 제거된 상태를 유지합니다.
- `--enable-automation` 안내 막대만 끕니다(`ignoreDefaultArgs`). 이 인자는 webdriver 값을 바꾸지 않습니다(실측).
- 워밍업 리듬: 하루 계획(3~9회를 07~10시 시작·21~24시 종료 구간에 무작위 산포 → 간격이 지수분포), 15% 쉬는 날, 세션 유형 3종(훑기/깊게 읽기/검색), 글 길이 비례 체류. 앱이 꺼져 있던 동안 지나간 예정(1시간 초과)은 버립니다.
- Codex 로컬 수정 포함: 모든 네이버 요청에 프록시 필수(`network-config.ts`), HTTPS 인증서 검사 복구, 빈 비밀번호 보존 + 명시적 삭제 체크박스, 워밍업 인증 검사(AUTH_STOP), 진단 문구에서 안전 보장 표현 제거.
- 검증: `npm run build`, `tests/session-safety.mjs`, `tests/offline-configuration-audit.mjs`, `tests/accounts-form.mjs` 통과. 실제 계정·네이버 접속·VM 테스트는 하지 않았습니다.
- 미확정: 네이버 보호조치의 직접 원인. getter `toString()`은 "() => false"로 보입니다(이전 배포본과 동일, 의도적으로 더 덮지 않음).

# v0.9.5 — 로그인 확인 지연 처리 및 열린 Chrome 유지

- 작업 시작 시 로그인 표시를 최대 8초 기다립니다. 쿠키만으로 로그인 성공 처리하지 않습니다.
- 로그인 확인 후 창을 닫지 않고 작업을 시작하도록 안내하며, 동일한 열린 컨텍스트를 작업에 넘깁니다.
- AUTH_STOP에 인증 쿠키 유무와 로그인/계정 UI 표시 여부를 남깁니다. 쿠키 값이나 개인정보는 기록하지 않습니다.
- 실제 계정 없이 로컬 지연 페이지와 임시 프로필 재실행으로 검증합니다.
- 사용자 VM의 실제 중단 원인과 네이버 보호조치 해소 여부는 미확정입니다.

# v0.9.4 — 로그인 세션 관리 수정

- 로그인과 작업의 Chrome 컨텍스트 관리를 통합하고 중복 실행을 방지합니다.
- 기존 로그인을 먼저 확인합니다. 브라우저 열기는 로그인 화면으로 강제 이동하지 않습니다.
- 인증 상태가 불확실하거나 끊기면 자동 재로그인하지 않고 작업을 중단합니다.
- 로그인 쿠키를 Electron으로 복사하거나 만료일을 변경하지 않습니다.
- 앱 종료/업데이트 시 Chrome 종료를 기다립니다.
- 로그인·수동 작업·자동 작업의 동시 조작을 차단합니다.
- 답변 작성 시간 초과 시 브라우저를 닫고 실행을 중단합니다.

검증: npm run build, node tests/session-safety.mjs (격리 환경, 실제 계정/네이버 서버 접속 없음).
실제 네이버 로그인 UI의 모든 변형 및 보호조치 해소 여부는 미검증입니다.
이 릴리즈에는 미완료 워밍업 일정/동작 변경을 포함하지 않습니다.
